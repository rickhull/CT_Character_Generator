#!/usr/bin/env ruby

require 'pry'
require 'traveller/generator'
require 'traveller/career_path'

char = Traveller::Generator.character
path = Traveller::CareerPath.new(char)

careers = %w{Scout}

loop {
  careers = careers.select { |c| path.eligible? c }
  if careers.empty?
    puts "No eligible careers available!"
    break
  end
  career = Traveller.choose("Choose a career", *careers)
  career = Traveller.career_class(career).new(char)

  # skip draft / drifter for now
  accepted = false
  accepted = path.apply(career) while !accepted

  #
  loop {
    path.run_term
    break unless path.active_career
    break if career.must_exit?
    next if career.must_remain?
    break if Traveller.choose("Muster out?", :yes, :no) == :yes
  }
  path.muster_out if path.active_career

  puts char.desc.pretty_inspect
  puts char.stats.pretty_inspect
  puts char.skills.pretty_inspect
  puts char.stuff.pretty_inspect

  break if Traveller.choose("Exit career mode?", :yes, :no) == :yes
}

binding.pry if ENV['PRY']

puts "DONE"
